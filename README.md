# REST Backend Service

Небольшой учебный backend-сервис на **Spring Boot**, предоставляющий REST-эндпоинты для авторизации пользователя и получения списка всех пользователей. Проект также включает автоматические тесты на **RestAssured** и интеграцию **Swagger** для документации API.

## 📁 Структура проекта

```
src/  
├── main/  
│ ├── java/guru/qa/restbackend/  
│ │ ├── RestBackendApplication.java # Точка входа, Swagger config  
│ │ ├── controller/BankController.java # REST-контроллер  
│ │ ├── domain/ # DTO  
│ │ └── exception/InvalidUsernameException.java  
│ └── resources/application.properties  
└── test/  
├── java/guru/qa/restbackend/  
│ ├── config/AuthConfig.java # Конфигурация тестовых данных  
│ ├── specs/BaseSpecs.java # RestAssured спецификации  
│ └── tests/ # Набор автотестов  
└── resources/auth.properties  
```


## 🚀 Функциональность

### **POST /user/login** — Авторизация
Принимает `LoginInfo` и возвращает информацию о пользователе при корректном логине (`userName = Sergey`).

### **GET /user/all** — Список пользователей
Возвращает статический список: `Dima`, `Alex`, `Dasha`.

## 🧪 Тестирование

Используется **JUnit5 + RestAssured**.

Покрыто три сценария:
- успешное получение всех пользователей
- успешная авторизация
- неуспешная авторизация

Тестовые данные берутся из `auth.properties`.

## 📘 Swagger UI

После запуска доступен по адресу: http://localhost:8080/swagger-ui/#/

<p>
<img title="Allure report" src="images/Swagger-UI.png">
</p>


## ▶️ Запуск проекта и тестов

Запустить проект в классе:
[RestBackendApplication.java](src/main/java/guru/qa/restbackend/RestBackendApplication.java)  
Или из терминала по команде
`
gradle bootRun
`

Запустить тесты из класса:
[BankControllerTest.java](src/test/java/guru/qa/restbackend/tests/BankControllerTest.java)  
Или из терминала по команде:
`
gradle clean test
`